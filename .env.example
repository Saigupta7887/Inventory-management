# Root env for docker-compose overrides (optional).
# The backend and frontend have their own .env.example files too.
POSTGRES_USER=bondly
POSTGRES_PASSWORD=bondly
POSTGRES_DB=bondly
SECRET_KEY=change-me-to-a-long-random-string
